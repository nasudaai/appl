# Todos

作業前か作業後に書くクセをつける。
## 11-11
yen_insert にエラー処理を追記した。引数の数を確認するコード。次に、引数の数を変数名に入れたい。各引数も変数に入れたい。引数は一旦一塊で受け取っている。各変数を用意してから受け取るか、配列化してからか。各変数を用意してから引数の位置パラメーターを指定するほうがそれぞれに受け入れ条件を書けるからよさそう。

git branch 関連のコマンドをファイルに軽くまとめておく。

### yen_insert の機能を他ファイルに 

- view_file.sh, today_data.sh, datetime.sh

### yen_insert に他ファイルの機能を

入力時の基本動作と、オプション的に使う場合がある

- calc.sh 系の機能を一部一旦 yen_insertへ

### その他

create_md.shを少し修正。受け取る引数への処理と表示を整える。

## 11-12

re_args.sh に引数を受け取る際の挙動を確かめるコードを書く。コマンドライン引数としてそのまま位置パラメータで処理するか配列化するか。

todo.mdの末尾に今日の日付を挿入する朝スクリプトを書くくらいが今はちょうどいいかも。

## 11-13

マークダウンを作成する、追記するコードも成長させよう。

missのパーセンテージをmainに置くパターンと、別で置くパターンで引数の位置が違う。一旦、mainにも置く。
2025-11-29 16:53:30
